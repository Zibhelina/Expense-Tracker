#include "../include/Journal_Entry.h"
#include <algorithm>
#include <sstream>
#include <string>

Journal_Entry::Journal_Entry(const std::string &date, double amount,
                             const std::string &category, Entry_Type type) {}
